import { ITableOption } from '../interface/table/Table'

export const defaultTableOption: Readonly<Required<ITableOption>> = {
  tdPadding: [0, 0, 0, 0],
  defaultTrHeight: 6,
  defaultTrMinHeight: 6,
  defaultColMinWidth: 40,
  defaultBorderColor: '#000000'
}
